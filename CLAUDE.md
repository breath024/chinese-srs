# 中文 회화 SRS — 인수인계서

> 다른 클로드 세션이 이 디렉토리를 이어받을 때 읽는 문서. Claude Code가 자동 로드함.

## 1. 정체

호윤이 중국인과 대화하기 위해 만드는 자기주도 회화 학습 앱.
**2026-05-27 시작 → 2026-05-29 현재 상용 수준 단일 HTML 앱으로 성장.**

특징:
- 단일 HTML 파일 + 분리된 데이터 팩 (외부 빌드 도구·서버 없이 더블클릭으로 실행)
- 호윤이 "다른 봇한테 데이터 추가 시키기 편하게" 분리해달라고 요청 → README.md에 AI 봇용 프롬프트 템플릿 있음
- iCloud나 동기화 없음. 모든 상태는 `localStorage`.

## 2. 파일 구조

```
중국어 공부/
├── index.html       # 학습 앱 (HTML + CSS + JS, ~2900줄)
├── manifest.json    # PWA manifest (홈 화면 추가용)
├── sw.js            # Service Worker (cache-first 오프라인 지원)
├── README.md        # 데이터 팩 추가 가이드 (사람용 + AI 봇용 프롬프트 템플릿)
├── CLAUDE.md        # 이 파일
└── data/
    ├── _packs.js    # window.SEED_PACKS = []. 항상 가장 먼저 로드.
    └── daily.js     # 일상 회화 50문장 (기본 시드 팩, w 단어 분절 포함)
```

**`index.html`의 `<!-- 팩 로드 -->` 주석 아래에 `<script src="data/[팩].js">` 한 줄 추가하면 새 팩이 머지됨.** 기존 학습 진행상황은 보존됨.

## 3. 핵심 데이터 모델

### Card (시드 팩 안의 1문장)
```js
{
  id: 'daily_001',          // 팩 내 고유. ★ 절대 변경 금지 (SRS 진행상황의 키)
  cat: '인사',              // 카테고리 (한국어)
  h: '你好。',              // 한자 + 구두점
  p: ['nǐ','hǎo'],          // 병음. 한자 글자 수와 같아야 (구두점·儿화 儿 제외)
  t: [3,3],                 // 성조 1~5. p와 동일 길이
  m: '안녕하세요.',          // 한국어 의미
  w: [                      // 옵셔널이지만 강력 권장 — 단어 분절
    { z:'你好', m:'안녕' }
  ],
  notes: '팁 한 줄 (선택)'
}
```

`loadCards()`가 시드 + 사용자 추가/편집/SRS 상태를 머지한 뒤 `zh_cards_v2`에 저장. 시드의 정적 필드(`h`/`p`/`t`/`m`/`w`/`notes`/`cat`)는 매 로드마다 시드의 최신 값으로 덮어쓰고, **SRS 상태(`ease`/`interval`/`due`/`reps`/`lapses`/`starred`/`note`)는 보존**한다.

### 儿화 처리
한자 직후의 `儿`은 음절 카운트에서 제외 (`一点儿` = 한자 3개지만 음절 2개 `yì·diǎnr`). `effectiveHanziCount()`와 `buildHanziGlyphs()`에 처리 들어가 있음. 단어 분절 작성 시 `一点儿` 한 단어로 묶으면 깔끔.

## 4. localStorage 키

| 키 | 용도 |
|---|---|
| `zh_cards_v2` | 머지된 카드 + SRS 상태 + 사용자 메모 |
| `zh_log_v1` | 일별 복습 횟수 `{'2026-05-28': 12}` |
| `zh_prefs_v1` | 방향/카테고리/속도/음량/테마/모드/알림 시간/onboarded |
| `zh_deleted_ids_v1` | 사용자가 삭제한 시드 ID — 다시 머지 시 부활 안 됨 |
| `zh_game_v1` | xp / goalXP / todayXP / lastSessionDate / bestStreak |

## 5. 기능 (2026-05-29 기준)

### 5-1. 학습 모드 5개 (학습 탭 상단 `mode-tabs` 토글)
- **🃏 카드** — 전통 SRS. 한→중(말하기) 또는 중→한(듣기·D키 전환)
- **🎯 퀴즈** — 4지선다. 같은 카테고리에서 distractor 우선 추출. 1~4키
- **🎧 듣기** — 한자 숨김 + 발음 자동재생 + 의미 4지선다
- **⌨ 타이핑** — 한국어 보고 병음 입력. 성조 부호 버튼 24개. 성조 부호 없이도 정답 인정 (`normalizePinyin()` — NFD로 분해 후 비교)
- **🔀 매칭** — 6~8쌍 한자/뜻 셔플 매칭. 타이머 + XP 보너스 + 컨페티

### 5-2. SRS (SM-2 단순화)
`gradeCard()`에 있음:
- 1(다시): interval=0, due=1분 후, ease −0.2, lapses+1
- 2(어려움): interval ×1.2, ease −0.15
- 3(좋음): interval ×ease
- 4(쉬움): interval ×ease ×1.3, ease +0.15

### 5-3. 게이미피케이션
- **XP**: 카드 1=+2, 2=+5, 3=+10, 4=+12 / 퀴즈 정답+8 오답+2 / 듣기 정답+10 / 타이핑 정답+12 / 매칭 짝+4 +완료보너스
- **레벨**: `floor(xp/100) + 1`. 칭호: 초보→입문(5)→익숙(10)→중급(20)→고수(30)→마스터(50)
- **데일리 골**: 기본 20 XP. 슬라이더 10~200. 달성 시 컨페티
- **연속일(streak)** + 최고 기록(`bestStreak`)

### 5-4. 발음
- Web Speech API (`zh-CN`)
- 카드 뒷면 인라인 컨트롤: 🔊/🐢/🐌 3단 속도, 속도·음량 슬라이더
- **단어 카드 클릭** → 단어만 발음 / **한자 글자 클릭** → 단음절 발음
- **🎤 음성 인식 따라하기**: `webkitSpeechRecognition` → Levenshtein 거리로 점수 (Chrome 권장)
- **병음→한글 자동 음역**: `pinyinToKor()` (initial+final 매핑 + 자모 합성)

### 5-5. 따라쓰기 (한자 캔버스)
카드 뒷면 "✍ 따라쓰기" 버튼 → 모달. 한자 가이드 위에 마우스/터치로 그리기. Retina(`devicePixelRatio`) 대응.

### 5-6. 명령 팔레트 (Ctrl/Cmd+K)
어디서든 호출. 한자/병음/뜻/카테고리/노트/한글음역 통합 검색 → 단어 상세 모달로 점프.

### 5-7. 단어 상세 모달 (역참조)
한 카드의 단어(`w[].z`)가 들어간 다른 모든 문장을 자동 검색.

### 5-8. PWA + 알림
- `manifest.json` + `sw.js` (cache-first)
- 매일 정한 시각에 브라우저 Notification (설정 → 알림 켜기 + 시간)

### 5-9. 기타
- 테마: 다크/라이트/시스템 (CSS 변수 `[data-theme]`)
- 카드별 메모(`card.note`)
- 즐겨찾기 ⭐ + ⭐ 칩으로 즐겨찾기만 학습
- 카드 모드 이전(`←`)/건너뛰기(`→`) + history 스택
- 컨페티 (canvas, 130 입자)
- 토스트 알림 시스템 (우상단 슬라이드인)
- 통계 탭: 도넛(신규/학습중/숙달) + 어려운 문장 Top 10 + 카테고리별 진행률 + 30일 히트맵 + 평균 ease + 레벨/XP 카드
- 첫 실행 온보딩 (4단계, `state.prefs.onboarded`)
- 학습 결과 1080×1080 PNG 이미지 저장 (설정에서)
- 카드 + 사용자 추가 카드 + 로그 + prefs를 JSON으로 export/import

## 6. 알려진 한계 / 주의

1. **file:// 프로토콜에서 Service Worker 등록 안 됨.** PWA·알림은 https:// 또는 localhost 호스팅 시에만 풀 작동. 호윤은 보통 더블클릭으로 여니까 SW는 그냥 fail-silent.
2. **음성 인식은 Chrome/Edge에서만 안정.** Safari/Firefox는 webkit 인식 없거나 제한.
3. **자동 한국어 음역(`pinyinToKor`)은 근사치.** 90%는 표준 한국 표기랑 비슷한데 ui/ue/r화 등 일부 음절은 부정확. 정확하려면 카드에 한국어 발음 필드(`k`)를 추가하고 우선 사용하도록 고치는 게 정공법.
4. **시드 카드의 `id`를 바꾸면 호윤의 진행상황이 사라진다.** ID는 한 번 정하면 절대 변경 금지. 의미가 바뀌면 새 ID로 새 카드 만들고 옛 ID는 `zh_deleted_ids_v1`로 가는 흐름.
5. **모든 모드가 같은 `state.queue`를 공유**하지만 매칭은 큐 무시하고 자체 풀에서 뽑음.
6. **컨텍스트 크기**: `index.html`이 2900줄대로 길어졌다. 큰 리팩토링이 필요해지면 모듈 분리(예: `js/srs.js`, `js/modes.js`)를 고려.

## 7. 호윤의 학습 패턴 / 선호 (관찰됨)

- **한국인 학습자 관점**: 한→중(말하기) 방향이 기본. 듣기 모드는 보조.
- **자기주도, 데이터 추가는 LLM에 위임**: 새 팩이 필요하면 README의 프롬프트 템플릿으로 다른 챗봇에게 시킴.
- **"완벽하게는 아니어도 쓸 수 있게"** 라고 명시함 (2026-05-29). 큰 기능을 MVP 수준이라도 빠르게 일단 넣는 걸 선호. 디테일은 나중에.
- **상용 수준의 디자인 디테일**을 원함. "팔만한 수준" 직접 언급. 그래서 컨페티·게이미피케이션·온보딩까지 진행됨.

## 8. 코드 컨벤션

- **단일 HTML 안의 `<style>` + `<script>`**. 외부 의존성 없음.
- 함수는 `function name() {}` 형태로 글로벌 스코프. 모듈 X.
- **`escapeHtml` / `escapeAttr`** 항상 사용자 데이터·카드 데이터에 적용 (XSS 방지).
- **CSS 변수**로 테마 분기. 새 색이 필요하면 `:root[data-theme="dark"]` / `[data-theme="light"]` 양쪽에 추가.
- **DOM 토큰 한자 범위**는 `/[一-鿿]/` (CJK Unified). 다른 범위 쓰지 말 것.
- 토스트는 `alert` 대신 `showToast({type, title, sub, icon})` 사용.

## 9. 다음 단계 후보 (호윤이 종종 추가 요청)

호윤이 "다음엔 뭐 할래"라고 하면 이 중에서 권장:
- **카드별 HSK 레벨 자동 태깅** (큰 사전 임베드 필요)
- **단어장 모드** — 카드와 별개로 단어 단위 독립 SRS
- **챗봇 대화 모드** — 시나리오 기반 대화 시뮬레이션
- **학습 통계 라인 차트** (일별/주별 XP 추이)
- **컬러 테마 다양화** (사쿠라/우주/포레스트 등)
- **카드별 태그 시스템**
- **공유 가능한 URL** (특정 카드로 deep link)
- **자동 클라우드 백업** (Google Drive/Dropbox 연동 — file:// 한계 때문에 어려움)
- **데이터에 `k` 필드 추가하고 정확한 한국어 발음 우선 사용** (자동 음역 보완)

## 10. 디버깅 팁

- 학습 진행이 꼬였다 싶으면: 설정 → 위험 영역 → "학습 진행상황 초기화"
- 새 시드 팩이 안 보이면: `index.html`에 `<script src>` 추가했는지 + 브라우저 캐시 새로고침 (Ctrl+F5)
- 모드별 state 꼬임: `switchMode()`에서 모든 모드 state 리셋함. 새 모드 추가하면 여기에도 리셋 추가 필수
- 음성 인식 안 됨: https나 localhost인지 + Chrome인지 + 마이크 권한 확인
